{
  "name": "refactoringminer-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action that detects refactorings and posts a PR comment",
  "main": "src/export.js",
  "scripts": {
    "build": "ncc build src/export.js -o dist/export --source-map --license licenses.txt && ncc build src/comment.js -o dist/comment --source-map --license licenses.txt",
    "test": "jest",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint src/ tests/ --fix"
  },
  "dependencies": {
    "@actions/artifact": "^2.2.2",
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
